# both claims are well formed and name nobody. the file is what a diagnostic
# about them has to reach
{ entry, ... }:
entry {
  furnish = {
    hosts = [ "workstatoin" ];
    users = [ "adda" ];
    files = [ ];
  };
}
